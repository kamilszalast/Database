package database;

import models.Contact;
import models.ContactEntity;

import java.sql.Connection;

public class ContactRepository extends ContactService {

    /**
     * Zapis do bazy danych
     */
    ContactEntity save(Contact contact) {

        return null;
    }
}
